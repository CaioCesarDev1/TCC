# ===================================================================
# OPÇÃO 1: MODO MOCK (SEM BANCO) - Recomendado para desenvolvimento
# ===================================================================
# Com ENABLE_MOCK_AUTH=true, você não precisa de banco de dados!
# A API funciona com dados mockados em memória.
#
# Credenciais mock:
# CPF: 12345678910
# Senha: 12345

DATABASE_URL="postgresql://postgres:senha123@localhost:5433/fhir_rac"
JWT_SECRET="chave-super-secreta-para-meu-tcc-minimo-32-caracteres"
PORT=3333
ENABLE_MOCK_AUTH=false
NODE_ENV=development

# ===================================================================
# OPÇÃO 2: PostgreSQL Local (via Docker) - Melhor para testes completos
# ===================================================================
# Rode: docker run --name fhir-postgres -e POSTGRES_PASSWORD=senha123 -p 5432:5432 -d postgres:14
# Depois: docker exec -it fhir-postgres createdb -U postgres fhir_rac
#
# DATABASE_URL="postgresql://postgres:senha123@localhost:5432/fhir_rac"
# JWT_SECRET="sua-chave-secreta-aqui"
# PORT=3333
# ENABLE_MOCK_AUTH=false
# NODE_ENV=development

# ===================================================================
# OPÇÃO 3: Supabase Free (limitado) - Não recomendado para migrations
# ===================================================================
# ATENÇÃO: Plano gratuito não tem Direct Connection (porta 5432)
# Session Pooler (porta 6543) NÃO funciona com migrations!
#
# DATABASE_URL="postgresql://postgres.xxxxx:senha@aws-x-xx-x.pooler.supabase.com:6543/postgres?pgbouncer=true"
# JWT_SECRET="sua-chave-secreta-aqui"
# PORT=3333
# ENABLE_MOCK_AUTH=false
# NODE_ENV=development


